{
  "name": "zoom-clone",
  "version": "1.0.0",
  "description": "Video conferencing platform with meeting and webinar modes",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["video", "conferencing", "webrtc", "zoom", "webinar"],
  "author": "Your Name",
  "license": "MIT"
}
